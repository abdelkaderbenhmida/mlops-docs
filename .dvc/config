[core]
    remote = storage
    analytics = false
['remote "storage"']
    url = s3://mlops-bucket/data
    endpointurl = http://localhost:9000
    access_key_id = minio
    secret_access_key = minio123
