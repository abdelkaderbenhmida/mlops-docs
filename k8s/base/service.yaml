apiVersion: v1
kind: Service
metadata:
  name: mlops-api
  labels:
    app: mlops-api
spec:
  type: ClusterIP
  selector:
    app: mlops-api
  ports:
    - name: http
      port: 80
      targetPort: http
